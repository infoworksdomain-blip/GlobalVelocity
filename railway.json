{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile" },
  "deploy": { "startCommand": "npm run db:migrate && npm run start", "healthcheckPath": "/api/health", "restartPolicyType": "ON_FAILURE" }
}
